#1) Use R to figure out how many elements in the vector below are greater than 2.

#rooms <- c(1, 5, 2, 3, 1, NA, 3, 1, 3, 2, NA, 1, 8, 3, 1, 4, NA, 1, 3, 1, 2, 1, 7, 1, NA, 4, 3 ,1 ,7 ,2 ,1 ,NA ,1, 1, 3)

#My Code ->
rooms <- c(1, 5, 2, 3, 1, NA, 3, 1, 3, 2, NA, 1, 8, 3, 1, 4, NA, 1, 3, 1, 
           2, 1, 7, 1, NA, 4, 3 ,1 ,7 ,2 ,1 ,NA ,1, 1, 3)

sum(rooms > 2, na.rm = TRUE)

#<-

#2) Which function tells you the type of data the 'rooms' vector above contains?

#The Question that is asked is kinda ambiguous as it doesnt specify what it really wants. If the function typeof(rooms) is used it will return as "double" since R stores numbers as double precision numbers. If the function mode(rooms) is used however it will show the way the data for "rooms" is stored in R which is in this case numerical. 
#My Code ->

mode(rooms)
typeof(rooms)

#<-


#3) What is the result of running the median() function on the above 'rooms' vector?

#If you run the median() function on the vector "rooms" it will return as NA as the vector contains "NA". If you want the median of the data "rooms" you will have to write the following code: mean(rooms, na.rm =TRUE)
#My code ->

median(rooms)
mean(rooms, na.rm = TRUE)
